{
  "version": "0.2.0",
  "configurations": [{
    "name": "Run Code Kanban",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
    "preLaunchTask": "npm: build"
  }]
}
